{
  "name": "root",
  "scripts": {
    "install-all": "cd frontend && npm install && cd ../backend && npm install",
    "build-all": "cd frontend && npx vite build",
    "start": "cd backend && npm start"
  }
}
